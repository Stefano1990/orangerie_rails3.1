<tr>
	<td><%= reservation_counter+1 %></td>
	<td><%= user_link(:user => reservation.user) %></td>
	<td><%= reservation.user.infos.sex %></td>
	<td><%= reservation.guests_male %></td>
	<td><%= reservation.guests_female %></td>
	<td><%= reservation.remarks %></td>
	<td><%= reservation.created_at.strftime("%A %d.%m.%y, %H:%M") %></td>
</tr>